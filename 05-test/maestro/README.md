# Maestro — Synced Step 9

Before running:
1. Replace `com.example.coloringapp`.
2. Implement stable native test IDs from `../selectors-contract.md`.
3. Install the APK/build.

Some editor tests remain templates until deterministic region/test hooks exist.
